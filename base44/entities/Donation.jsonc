{
  "name": "Donation",
  "type": "object",
  "properties": {
    "donor_name": {
      "type": "string",
      "description": "Name of the donor"
    },
    "donor_email": {
      "type": "string",
      "description": "Email of the donor"
    },
    "donor_user_id": {
      "type": "string",
      "description": "Base44 User.id of the donor if they are a logged-in member"
    },
    "amount": {
      "type": "number",
      "description": "Donation amount in dollars"
    },
    "fund": {
      "type": "string",
      "enum": [
        "general",
        "building_campaign",
        "missions",
        "youth",
        "community_meals",
        "people"
      ],
      "description": "Which fund the donation goes to. 'people' = gift to an individual via Care & Support board."
    },
    "donation_date": {
      "type": "string",
      "format": "date",
      "description": "Date the donation was made (YYYY-MM-DD)"
    },
    "recipient_name": {
      "type": "string",
      "description": "Name of the individual recipient for 'people' fund gifts"
    },
    "is_recurring": {
      "type": "boolean",
      "default": false
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "donor_name",
    "amount",
    "fund"
  ]
}